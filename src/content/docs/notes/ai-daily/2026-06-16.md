---
title: AI云组会 2026-06-16
description: 抓取失败 — RSSHub 服务不可用
---

## ⚠️ 今日简报未能生成

**原因**：本地 RSSHub 服务（`localhost:1200`）未能启动。

- Docker daemon 未运行，无法通过 `docker-compose` 启动 RSSHub
- 直接 `tsx lib/index.ts` 启动无输出、无监听端口（进程存活但无日志写入）
- 该问题自 2026-06-12 起持续存在

**影响范围**：全部 46 个关注账号均无法抓取

**上次成功运行**：2026-06-15（日志显示正常返回 200）

**建议修复**：
1. 检查 `.env` 中 `TWITTER_AUTH_TOKEN` 是否仍有效
2. 尝试 `docker-compose up -d` 启动完整服务栈（含 Redis）
3. 或手动 `tsx lib/index.ts` 并在另一终端验证 `curl localhost:1200/healthz`
